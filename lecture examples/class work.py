def collection_examples() -> None:



    def get_external_ip() -> str.:
        return r.compile(r'Address: (\d+\.\d+\.\d+\.\d+)')
    .search(str(urlopen('http://checkip.dynds.com/').read())).group(1))

def get_iternal_ip() -> str:
    s = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
    s. connect(("8.8.8.8", 80))
    ip = s.getsockname()[0]
    s.close()
    return ip

def handle_client(conn,ddr):


courses = ['history,', Math', 'Physics', 'CompSci]
courses_2 = ['Art', 'Education']

courses.extend(courses_2)
print(courses)





courses = ['history,', Math', 'Physics', 'CompSci]
courses_2 = ['Art', 'Education']

courses.sort()
courses_2.sort()
print(courses)





courses = ['history,', Math', 'Physics', 'CompSci]
courses_2 = ['Art', 'Education']

courses.extend(courses_2)
print(courses)


def collection_examples() -> None:


courses = ['history,', Math', 'Physics', 'CompSci]
courses_2 = ['Art', 'Education']

sorted_courses = sorted(courses)
print(courses)

def get_external_ip() -> str.:
    return r.compile(r'Address: (\d+\.\d+\.\d+\.\d+)')
